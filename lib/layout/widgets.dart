import 'package:flutter/material.dart';

TextStyle butonTextStyle(
    {Color color = Colors.black,
    double fontSize = 11,
    fontFamily = 'RotisSemiSnas'}) {
  return TextStyle(color: color, fontSize: fontSize, fontFamily: fontFamily);
}
